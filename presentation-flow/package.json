{
  "name": "sail-digital-pulse-presentation-flow",
  "version": "1.0.0",
  "description": "SAIL Digital Pulse — Real-time Workshop Intelligence",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install && npm install --legacy-peer-deps --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "socket.io": "^4.7.5"
  }
}
